//require('nodetime').profile();

var RepeatProto = require("./repeat");

var i = 1000;
while (i--) {
  new RepeatProto(function() {}).every(2, 'ms').for(20, 's').start();
}

new RepeatProto(function() {console.log("waiting...")}).every(11, 's').for(33, 's').start();